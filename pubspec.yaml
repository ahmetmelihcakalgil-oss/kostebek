name: kostebek
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Flutter SDK versiyonuna göre ayarlayabilirsin

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5 # Provider paketi

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true